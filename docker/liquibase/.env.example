# Liquibase docker/podman-compose configuration (copy to .env)
#
# This file is optional. If you create docker/liquibase/.env, the Make targets
# and documented compose commands will load it when present.

# Optional: Join an existing external container network (ONLY for network mode).
# Leave blank for standalone mode (external DB via DNS/IP).
DBTOOLS_NETWORK=

# Optional: On SELinux systems (RHEL/Fedora) using Podman, bind mounts often need a label.
# Use ':Z' (private) in most cases.
LIQUIBASE_VOLUME_SUFFIX=:Z

# Optional: If you want files written to ./output to be owned by your user.
# In a rootless Podman environment, set these to your host UID/GID.
# LIQUIBASE_UID=1000
# LIQUIBASE_GID=1000
