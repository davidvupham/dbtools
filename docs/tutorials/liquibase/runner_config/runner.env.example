# Example GitHub Runner Environment File
# Copy this file to `runner.env.local` (recommended) or `runner.env` outside the repo.
# NEVER commit a file containing a real RUNNER_TOKEN.
#
# Required values
REPO_URL=https://github.com/ORG_NAME/REPO_NAME
RUNNER_TOKEN=REPLACE_WITH_EPHEMERAL_REGISTRATION_TOKEN

# Optional / tunable values
RUNNER_NAME=wsl-docker-runner
RUNNER_LABELS=self-hosted,linux,x64,wsl,docker
RUNNER_GROUP=default
RUNNER_WORKDIR=/tmp/github-runner

# Usage:
#   cp runner.env.example runner.env.local
#   edit runner.env.local to fill in real values
#   source runner.env.local
#   export RUNNER_NAME RUNNER_WORKDIR RUNNER_TOKEN REPO_URL RUNNER_LABELS RUNNER_GROUP
#   docker-compose --profile runner up -d github_runner
